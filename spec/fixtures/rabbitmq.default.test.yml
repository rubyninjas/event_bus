test:
  connection:
    host: 127.0.0.1
    port: 5672
    user: guest
    pass: guest
  exchanges:
    some:
      name: mst.some.test
      routing_key: mst.some
      bind_queue: 'mst.scorecard_live.test'
      delay: 5_000
    some_other:
      name: mst.some.other.test
      routing_key: mst.some.other
      bind_queue: 'mst.scorecard_live.test'
      delay: 5_000
